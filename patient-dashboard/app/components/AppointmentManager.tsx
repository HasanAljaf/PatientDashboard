// Shared Imports
import { ToggleVisibilityProps } from '../sharedPropTypes/ToggleVisibilityTypes';

export default function AppointmentManager({ style }: ToggleVisibilityProps) {
  return (
    <div style={style}>
      <h1>Appointment Manager</h1>
    </div>
  );
}
